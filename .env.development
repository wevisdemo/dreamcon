# Loaded automatically by `vite` in dev mode (not by `vite build`).
# Points the app at the local Firebase emulators so local development can never
# read or write the production project.
VITE_USE_FIREBASE_EMULATOR=true
VITE_BASE_URL=http://localhost:5173
